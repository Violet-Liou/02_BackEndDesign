@{
	//5.3.1 在_ViewStart.cshtml中加入前台及後台使用不同Layout的判斷式
	if (User.IsInRole("Admin"))
	{
		Layout = "_AdminLayout";
	}
	else if (User.IsInRole("User"))
	{
		Layout = "_UserLayout";
	}
	else
	{
		Layout = "_GuestLayout";
	}

    Layout = "_Layout";
}
